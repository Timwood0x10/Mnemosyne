# 全库评审报告（2026-09-22）— 认知状态层

> 评审范围：`src/` 全量阅读 + `docs/zh/dev-plan-cognitive-state.md`（认知状态层计划）对照 + 关键缺陷**实证复现**。
> 方法：先按模块通读，再对可疑点编写一次性探针测试运行验证（验证后已删除探针）。
> 结论：计划三步（Fact 升维 / 状态区间 / Decision）代码均已提交，但 **Step 2 在真实数据上失效、Step 3 只有读没有写**。
> 本文件为只读评审记录，不参与构建。
>
> **全部缺陷已于同日修复**（见 §2 各项、§3 修复表、§5 验证结果），并追加完成
> 模块拆分与后续工作（见 §6）。版本升至 `0.1.3`。

---

## 1. 项目定位与当前进度

**定位**：Mnemosyne 是 LLM-free 的认知记忆引擎 + MCP 服务，把对话编译为
`Fact → Evidence → State → Transition → Decision`，为陪伴型 AI 提供跨会话一致的人格。
核心承诺：确定性、零 API key、可溯源（EvidenceRef）。

| 计划步骤 | 提交 | 代码状态 | 生产可用性 |
|---|---|---|---|
| Step 1 `Fact` 升维（`confidence`/`status`/`derived_from`）+ `fact_provenance` | `61cc589` | 已实现 | 可用 |
| Step 2 状态区间（`state_intervals`）+ `state_timeline` | `5022515` | 已实现 + 已修复 | **可用**（H1/H2 已修） |
| Step 3 `Decision`（`decisions` 表 + `decision_trace`/`decision_search`） | `922289c` | 读写闭环 | **可用**（M1 已补编译期写入） |

判断：**当前应做的是"补齐 + 验证 + 收口"，而不是引入新设计。**

---

## 2. 缺陷清单

### 🔴 H1 — `state.rs` 变迁索引恒指向最后两个区间（已实证）

```rust
// src/state.rs:184-196（修复前）
for window in evolution.intervals.windows(2) {
    let (from, to) = (&window[0], &window[1]);
    if let Some(transition_type) = detect_transition(from, to) {
        evolution.transitions.push(StateTransition {
            from_index: evolution.intervals.len() - 2,
            to_index: evolution.intervals.len() - 1,
```

`evolution.intervals` 在循环内**长度不变**（push 的是 `transitions`），因此每条变迁都被写成
`(len-2, len-1)`：仅当区间数为 2 时恰好正确，≥3 时全部指向末尾。

**实证**：3 个区间 + 2 处渐变，实际得到 `[(1,2), (1,2)]`，期望 `[(0,1), (1,2)]`。

**漏检原因**：现有测试的变迁用例全部只有 2 个区间。

**修复**：`windows(2).enumerate()`，写 `from_index = i`、`to_index = i + 1`。

---

### 🔴 H2 — `state_timeline` 在真实数据上返回空（已实证）

`intervals_for_dimension` 以 **payload 是否含某个字段** 作为维度入选条件，
而 `COGNITIVE_DIMENSIONS` 使用的字段在生产链路中**从不存在**：

| 生产来源 | 实际 payload 字段 |
|---|---|
| `src/observation_compiler.rs` | `{action, subject, object}` |
| `src/agent_personality.rs` | `{attribution, content, negated}` |
| `src/persona/mod.rs` | `{attribution, speaker, content, negated, confidence}` |
| `src/agent_facts.rs` | `{attribution, content, confidence, evidence}` |
| `src/story_bridge.rs` | `{content, source, character}` |

对比 `StateEngine::aggregate()`（`src/cognition.rs`）使用的是**真实**键：
`goal/content`、`topic/preference/content`、`target/with/object/content`、
`attribute/identity/key/content`，且 emotion 是**按 `fact_type` 过滤**而非 payload 键。

即：`aggregate()` 认识真实数据，`COGNITIVE_DIMENSIONS` 不认识。

**实证**：喂入 `agent_personality` 形状的 Emotion facts，`aggregate_intervals` 返回 **dimensions = 0**。

**修复**：维度改为按 `FactType` 过滤，值键改为 `["content", <维度回退键>]`，
无匹配键时回退到完整 payload（保证不同事实不折叠）；`StateEvolution.key` 用 `FactType::as_str()`。

---

### 🟠 M1 — Decision 只有读、没有写，计划 Step 3 的验收不可达

`insert_decision` / `set_decision_outcome` 在全库**仅测试调用**；已注册工具只有
`decision_trace`、`decision_search`（均只读）；编译器也没有从"承诺语句"生成 Decision 的路径。
计划 §4 Step 3 的验收句（编译含承诺的对话 → `decision_trace` 回溯）当前走不通。

**状态**：待产品决策（新增写工具 vs 编译期自动生成），本轮不擅自新增工具。

---

### 🟠 M2 — `set_decision_outcome` 非原子，"只应用一次"承诺不成立

原实现为「读 → 判空 → 写」跨两次独立加锁，并发下第二个调用会**覆盖**第一个 outcome
（`fulfilled` 被 `violated` 冲掉）。单线程测试无法暴露。

**修复**：把守卫下推到 SQL —— `UPDATE ... WHERE id = ?2 AND outcome IS NULL`。

---

### 🟡 L1 — `decision_search` 的 `limit` 未钳制

Schema 声明 `maximum: 100`，handler 未钳制（与库内其他工具的钳制惯例不一致）。

### 🟡 L2 — `search_decisions` 的 LIKE 通配符未转义

`format!("%{}%", keyword)` 未转义 `\` / `%` / `_`，与审计第二轮已修的
`knowledge/store.rs::search_evidence`/`search_objects` 同类，此处被漏掉。

### 🟡 L3 — `confidence` 与 `decay` 压在同一列，违反计划 §2.3 正交性

`row_to_fact` 把 `weight` 读作 `confidence`，而 `set_decay` 写 `weight`：
每次 decay 都会改写"认知置信度"。计划要求 `confidence ≠ status ≠ decay` 三者独立。
**状态**：设计债，建议排入 v0.4，本轮不动（改动面大且涉及数据迁移）。

### 🟡 L4 — `validate_decision` 是死代码

只有测试调用，`insert_decision` 不做校验。

**修复**：接入 `insert_decision`。

### 🟡 L5 — 文档 / 元数据陈旧

- README.md、README.zh.md、`docs/**/mcp-tools.md` 均**未提及**
  `fact_provenance`、`state_timeline`、`decision_trace`、`decision_search`；
- `src/lib.rs` 顶部仍写 "exposes 10 MCP tools"（与实际 ~32 个工具定义不符）；
- `Cargo.toml` 仍是 `0.1.2`；`release.md` 称 "33+ tools"，与 lib.rs 的 10 自相矛盾；
- 提交 `922289c` 标题为 "feat(paper-lab)..."，实际内容是 Decision 全套实现（标题与内容不符）。

---

## 3. 本轮修复计划与状态

| 编号 | 严重度 | 修复内容 | 状态 |
|---|---|---|---|
| H1 | High | `state.rs` 变迁 `from_index`/`to_index` 取窗口自身下标 | ✅ 本轮 |
| H2 | High | 维度按 `FactType` 过滤 + 值键对齐生产 payload + 完整 payload 回退 | ✅ 本轮 |
| M2 | Medium | `set_decision_outcome` 守卫下推 SQL（`AND outcome IS NULL`） | ✅ 本轮 |
| L1 | Low | `decision_search` limit 钳制到 schema 上限 | ✅ 本轮 |
| L2 | Low | `search_decisions` LIKE 转义（`\` → `%` → `_`，`ESCAPE '\'`） | ✅ 本轮 |
| L4 | Low | `insert_decision` 接入 `validate_decision` | ✅ 本轮 |
| M1 | Medium | Decision 写入路径：编译期从承诺语句生成 `Decision`（不新增工具，见 §6） | ✅ 本轮 |
| L3 | Low | `confidence` / `weight` 拆列（正交性，含旧库回填迁移） | ✅ 本轮（见 §7.3） |
| L5 | Low | README / docs / 版本号收口（→ `0.1.3`） | ✅ 本轮 |

### 回归测试补齐（针对 H1/H2 的测试盲区）

| 测试 | 位置 | 锁死的不变量 |
|---|---|---|
| `every_transition_references_its_own_window` | `src/state.rs` | 3 个区间 → 2 条变迁，索引必须是 `(0,1)`/`(1,2)`（H1） |
| `production_shaped_payloads_map_to_their_dimension` | `src/state.rs` | `{attribution, content, negated}` 形状的 Emotion facts 必须命中原生维度（H2） |
| `aggregate_intervals_maps_production_shaped_payloads` | `src/cognition.rs` | 引擎级：key-less payload 必须产出 goal/preference/emotion 三个维度（H2） |
| `insert_decision_rejects_invalid_fields` | `src/fact_store.rs` | `validate_decision` 真正接入写路径，非法决策不入库（L4） |
| `decision_outcome_guard_is_atomic_at_the_statement_level` | `src/fact_store.rs` | 首次 outcome 变更 1 行、二次变更 0 行、未知 id 变更 0 行（M2） |
| `search_decisions_escapes_like_wildcards` | `src/fact_store.rs` | `%` / `_` / `\` 均按字面量匹配（L2） |

---

## 4. 验证结果

| 检查 | 结果 |
|---|---|
| `cargo fmt --all --check` | ✅ 干净（FMT_CLEAN） |
| `make check`（clippy + check，`--all-targets --all-features`） | ✅ 0 error |
| `cargo test --lib` | ✅ 767 passed / 0 failed |
| `make test`（nextest，lib + 全部集成测试） | ✅ 779 passed / 0 failed / 9 skipped |
| 新增 6 个靶向回归测试 + 10 个 Decision 写入路径测试 | ✅ 全部通过 |

改动清单：`src/state.rs`、`src/cognition.rs`（拆为 `src/cognition/`）、`src/fact_store.rs`（拆为 `src/fact_store/`）、`src/mcp/decision_tool.rs`、新增 `src/commitment.rs`。

---

## 5. 遗留与未做的事（明确声明）

- **未执行任何 `git` 命令**（遵循 `plan/rules/rules.md` 第 6 条）。
- **未删除任何文件、未执行任何 `rm`**（遵循第 1 条）。
- **未改动 `memory_decay` 的衰减策略本身**（打分/归档规则不变）；仅把 `confidence`
  从 `weight` 拆出，使衰减不再改写置信度。M1 采用冻结计划的方案（编译期生成，不新增工具）。

---

## 6. 模块拆分与后续工作（同日续作）

### 6.1 规则 1：单文件 ≤ 1000 行 —— 全库达标

拆分为 11 个模块目录 / 子模块，拆分后经 `make check`（0 error）+ `make test`（779 passed）验证，
功能零折扣：

| 原文件 | 行数 | 拆分结果 |
|---|---|---|
| `knowledge/store.rs` | 2851 | `store/{mod,types,trait_def,queries}.rs` + `store/tests/{mod,views}.rs`（最大 884） |
| `fact_store.rs` | 1639 | `fact_store/{mod,entities,decisions,relationship}.rs`（最大 738） |
| `lexicon/mod.rs` | 1557 | `lexicon/{mod,registry,matcher}.rs`（最大 884） |
| `distiller.rs` | 1510 | `distiller/{mod,pipeline,text}.rs`（最大 934） |
| `store.rs` | 1247 | `store/{mod,repository}.rs`（最大 894） |
| `main.rs` | 1215 | `main.rs` + `{legacy_memory_tools,character_tools}.rs`（最大 727） |
| `cognition.rs` | 1089 | `cognition/{mod,state,snapshot}.rs`（最大 753） |
| `ingest/characters.rs` | 1060 | `characters/{mod,data}.rs`（最大 950） |
| `compiler/name_validation.rs` | 1056 | `name_validation/{mod,fallback}.rs`（最大 927） |
| `conversation_compiler.rs` | 1015 | `conversation_compiler/{mod,markers}.rs`（最大 842） |
| `retrieval.rs` | 1012 | `retrieval/{mod,engine}.rs`（最大 622） |
| `character.rs` | 1011 | `character/{mod,store}.rs`（最大 574） |

拆分要点：

- **单元测试留在各自文件的末尾**（规则 4.1）——`fact_store` 与 `knowledge/store` 因为测试体量大，
  改为 `tests/` 子模块（`tests/mod.rs` 持有共享 fixture，`tests/views.rs` 持有视图测试）。
- **`knowledge/store.rs` 的 1095 行单 trait impl 无法跨文件拆分**（实测 `E0119`
  conflicting implementations），因此把 `inspect_entity` / `relation_graph` /
  `entity_timeline` / `search_evidence` / `find_object_by_alias` /
  `update_object_properties` / `get_evidence_for_many` 的**方法体原样**下沉为同类型的
  `pub(super)` 固有方法（`store/queries.rs`），trait 方法改为一行委托——`self` 语义不变，
  零逻辑改写。
- 顺带消除重复：新增 `FactType::as_str()`，替换三处 `fact_type_name` 映射。

### 6.2 M1：Decision 写入路径（编译期生成，不新增工具）

沿用冻结计划 §4 Step 3「只两个读工具、不扩展」的约束，把写入放在**编译期**：

- 新增 `src/commitment.rs`：规则驱动的承诺抽取（`答应/承诺/保证/发誓/说好了` +
  `promise/i'll/i will/swear`），刻意保守——单独的「我会…」是**计划**（已编译为 Goal 事实），
  不算承诺。
- `memory_compile` 在写完事实后，把承诺编译为 `Decision` 并落库；**承诺话语本身先落一条
  Event 事实作为锚点**，`Decision.because` 指向该锚点（并补充同主体可匹配的既有事实），
  使 `decision_trace` 能真正回溯到证据。
- 新增可选参数 `agent_id`：仅当调用方传入时才会解析 agent 实体并抽取 agent 自身的承诺，
  避免无故物化 agent 实体。

> 实证发现：承诺语句原本**不产生任何事实**（marker 表不含承诺词），所以「先锚定事实、
> 再指向事实」是让 `because` 有意义的前提，而不是可选项。

### 6.3 L5：版本与文档收口

- `Cargo.toml`：`0.1.2` → `0.1.3`；`CHANGELOG.md` 的 `[Unreleased]` 定稿为 `[0.1.3] - 2026-09-22`。
- `release.md`：版本头与 changelog 更新为 `0.1.3`。
- `src/lib.rs` 模块文档不再声称 "10 MCP tools"。
- `README.md` / `README.zh.md`：工具表补齐 `state_timeline`、`fact_provenance`，
  并新增「决策工具」小节（`decision_trace` / `decision_search`）。

### 6.4 最终验证

| 检查 | 结果 |
|---|---|
| `cargo fmt --all --check` | ✅ 干净 |
| `make check`（clippy + check，`--all-targets --all-features`） | ✅ 0 error |
| `cargo test --lib` | ✅ 767 passed / 0 failed |
| `make test` | ✅ 780 passed / 0 failed / 9 skipped |
| 超标文件（>1000 行） | ✅ 0 个 |

---

## 7. 后续：端到端验证与文档欠账（第二轮续作）

### 7.1 新增 `tests/cognitive_state_e2e.rs`（走真实 MCP 路径）

仓库此前**所有**集成测试都直接调用 `ToolHandler`，绕过了服务器的方法分派、
`tools/call` 参数校验与结果封装——这也正是 H1/H2 能长期隐藏的原因（测试全用
手造 payload）。新测试通过实现 `mcp::Transport` 的内存传输驱动
`MCPServer::serve`，跑的是客户端真正说出口的协议：

```
memory_compile ──► facts + 1 条锚定到 Event 事实的 Decision
      ├─► state_timeline   （goal / emotion 维度 + 区间）
      ├─► decision_search  （找到该决策）
      ├─► decision_trace   （回溯到锚点事实）
      └─► fact_provenance  （active / confidence / 空推导链）
```

**不预置任何数据**：被测事实全部由工具调用自身产生。该测试在 H2 未修复时必然失败
（`dimensions` 为空数组），因此是真实回归护栏。

### 7.2 文档欠账清理

- `docs/{zh,en}/mcp-tools.md`：概览表补齐 4 个新工具；新增
  `state_timeline` / `fact_provenance` / `decision_trace` / `decision_search`
  各一节（输入模式 + 请求/响应示例 + 行为约束）；删除「10 个 MCP 工具」的过时说法，
  改为指向 README 的权威清单（该文件本就只覆盖历史核心集）。
- `README.md` / `README.zh.md`：Testing 与 Test Corpora 两节重写为
  真实状态——套件自包含（无 fixture 依赖）、测试数修正、删除不存在的
  `sanguo_compile` / `generalize_corpus_regression` 等引用。
- `Cargo.toml`：新增 `async-trait` 到 `[dev-dependencies]`（集成测试实现
  `Transport` 所需）。

### 7.3 L3：`confidence` 与衰减拆列（计划 §2.3 正交性）

拆列前：`row_to_fact` 把 `weight` 读成 `confidence`，而 `set_decay` 写的正是
`weight` —— **每次归档/衰减都在悄悄改写"我们有多相信这条事实"**，与计划
`confidence ≠ status ≠ decay` 冲突。

改动（`src/fact_store/mod.rs`）：

| 项 | 处理 |
|---|---|
| schema | `facts` 新增 `confidence REAL NOT NULL DEFAULT 1.0`，与 `weight`（衰减）并存 |
| 迁移 | `ensure_column` 改为返回「是否新建」，新建时一次性 `UPDATE facts SET confidence = weight` 回填，旧库不丢置信度 |
| 读取 | `row_to_fact` 读 `confidence` 自有列；5 处 SELECT + 2 处 INSERT 列名同步 |
| 写入 | `insert_fact`/`insert_batch` 不再把 `confidence` 塞进 `weight`，`weight` 走默认值 1.0 |
| decay | `set_decay` 只写 `weight`/`archived`，**不碰** `confidence`/`status` |

新增 2 个回归测试：

- `decay_does_not_change_confidence_or_status`：confidence 0.42 的事实被归档后，
  confidence 仍为 0.42、status 仍为 `Active`，而 `get_decay` 正确返回 `(0.25, true)`。
- `legacy_facts_backfill_confidence_from_weight`：模拟无 `confidence` 列的旧库
  （`weight = 0.4`），重跑 schema init 后 `confidence == 0.4`。

影响面核查：`decay.rs` 不读 `confidence`；`personality.rs` 排序的是自算的
`Trait` 分数而非 `Fact.confidence`；其余 `.confidence` 命中均为另一类型
`Experience`。故除"衰减不再改写置信度"这一预期变化外，行为等价。

### 7.4 计划文档同步（已完成）

`docs/zh/dev-plan-cognitive-state.md` 已按实施结果修订，消除与现实的偏差：

| 项 | 修订 |
|---|---|
| 标题/状态 | 去掉版本号绑定，改为「Step 1/2/3，随 0.1.3 发布」 |
| §1.3 | 「版本定义」→「阶段定义」 |
| §2.3 | 正交性原则补充落地方式（三者各自独立列 + 两条锁死不变量） |
| §3.3 | `StateInterval` 补齐 `state_value`/`fold_key`/`fact_ids`；`Vec<StateInterval>` → `Vec<StateEvolution>` |
| §3.x Decision 模型 | `subject: String` → `i64`、`object: Option<String>` → `String`（与实现对齐） |
| §4 Step 1 | 文件路径改为 `src/cognition/`、`src/fact_store/`；`confidence` 由「映射 `weight`」改为独立列 + 回填 |
| §4 Step 2 | 文件路径与 `state_timeline` 入参（`entity_id`）对齐 |
| §4 Step 3 | 写入路径修正为**编译期生成 + Event 事实锚点**；`decision_search` 入参对齐 |
| §5 风险表 | 两行改为描述实际的独立列 + 回填 + 锁死不变量 |
| §8 | 「里程碑」→「实施状态」 |
| §9 评审记录 | 版本措辞改为 Step 措辞 |

校验：文档内引用的 12 个 `src/...` 路径**全部存在**；过时的版本号 /「双读」/「700+」
等表述已零残留（含源码注释中的同名措辞）。

### 7.5 仍未做

无。除计划文档本身记录的 §6 scope 红线（Ontology / Graph DB / LLM judge 等明确不做）外，
本轮审计与后续工作项已全部关闭。
