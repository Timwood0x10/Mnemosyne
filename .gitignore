/target

plan/

*.db

.codescope/
.workbuddy/

.fastembed_cache/

**/*.pyc

# Test corpora are large third-party text; keep them out of the repo (tests
# skip gracefully when a corpus file is absent). Docs, config, and source
# MUST be tracked.
corpus/*.pdf
corpus/*.txt
corpus/*.json

# memory_export/import 的导出目录：导出的记忆快照可能含用户数据，
# 禁止提交进仓库。
exports/
