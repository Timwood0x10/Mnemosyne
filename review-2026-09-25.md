# Code Review（第三轮）— 配置 / 词表加载层 + 当前门禁状态

> 评审日期：2026-09-25 · 版本：`0.1.3` · 依据 `plan/rules/rules.md`
>
> 目的：在动手做"用户可配置词表"之前，先审清这一层的现状。
>
> **状态：C1–C6、C8–C10 已修复，C7/C11 与一项词表数据决定留在 §8.3（见 §8）。**

## 0. 范围与边界（先说清楚我审了什么、没审什么）

**深审**：`src/config.rs` 的资源路径解析 + 全部 13 个资源加载器 + `config/markers_zh|en.json` 词表本身。

**机器扫查（全库）**：`make check` / `make test` / `cargo fmt` / 单文件行数 / 非测试区 `unwrap|expect|panic` / `#[allow]` 数量。

**未做语义审查**：上次评审（`review-2026-09-23.md`）之后有一批文件被改动过，我只做了机器扫查，**没有**逐行审。清单见 §6 —— 需要的话我单独开一轮。

**未执行任何 git 命令**（规则 6）；本轮**未修改任何源码或数据**（只读检查 + 本报告）。

## 1. 门禁现状

| 检查 | 结果 | 说明 |
|---|---|---|
| `cargo fmt --all --check` | ✅ clean | — |
| `make check` | 0 error / **2 warning** | 规则 4 允许暂时不管，根因与修法见 §5.3 |
| `make test`（nextest） | ✅ **824 passed / 0 failed / 9 skipped** | — |
| 单文件 ≤1000 行（规则 1） | ✅ 但**贴线** | 最大三个：`src/compiler/pipeline.rs` **994**、`src/compiler/profile.rs` **978**、`src/knowledge/store/mod.rs` **964**（见 C11） |
| 非测试区 `unwrap()/expect()/panic!` | ⚠️ 51 处 | 绝大多数是 `.expect("lock not poisoned")` / `expect("… never fail")`，符合规则"能证明不会失败并注明理由"；仅 2 处值得改（§3 C9） |
| `#[allow(...)]`（规则 5） | ⚠️ 4 处新增 | 均为 `clippy::too_many_arguments`，不是规则 5 明令禁止的 `dead_code`，故**不算违规**；但见 §3 C7 |

## 2. 这一层的结论（一句话）

**"外部 JSON + fail-soft 内置默认"这个方向是对的，但它目前只走到"用户能编辑随包文件"这一步。再往前走的三件事——改错有反馈、能覆盖/关闭默认词、能把词表放到别处——全都是断的。**

所以现状离你要的"简单配置，就能直接用"，缺的不是词表本身（词表已经在 `config/` 里了），而是**这套配置的可用性**：反馈、覆盖、位置。

## 3. 发现（按严重度）

### C1 · High — 桶名写错会**静默**变成 Event 事实

证据链（三处代码合起来才看得出来）：

1. 加载器只认 JSON 结构，不认桶名 —— `markers.rs:50-64` 把任意 key 当 action 直接 push 成 `(词, action)`；
2. action 被当作"动词"送进映射 —— `observation_compiler.rs:171,176`：`let verb = &observation.action; verb_to_fact_type(verb)`；
3. 映射先查词典、再走硬编码兜底，**兜底是 `_ => FactType::Event`** —— `observation_compiler.rs:250`。

后果：用户把 `feel` 打成 `feell`，那些词**不会报错、也不会被丢弃**，而是全部变成 `Event` 事实塞进 `recent_events`。用户改坏了词表，得到的是"看起来在动、其实全错"的结果。

补充证据：`config/dictionary.json` 只有 126 个 lexeme，其中**不存在** `belief` / `stuck` / `life_event` / `plan` / `准备` / `喜欢`，所以这些 bucket 完全不靠词典，只靠硬编码列表。

### C2 · High — 内置默认词表与随包 JSON **已经漂移**，且 fallback 缺 3 个桶

`markers.rs:85` 的 `DEFAULT_OBSERVATION_MARKERS` 是随包 JSON 的第二份拷贝，注释写着 "Mirrors the shipped `config/markers_zh.json` + `config/markers_en.json`"。实测**并不 mirror**：

| action | Rust 默认 | JSON | | action | Rust 默认 | JSON |
|---|---:|---:|---|---|---:|---:|
| 喜欢 | 10 | 27 | | want | 8 | 26 |
| like | 1 | 12 | | belief | **0** | 26 |
| love | 1 | 8 | | stuck | **0** | 27 |
| dislike | 4 | 30 | | life_event | **0** | 41 |
| plan | 7 | 28 | | feel | 59 | 191 |
| 准备 | 1 | 4 | | **合计** | **91** | **420** |

后果：JSON 缺失/损坏时，引擎静默降级到一份 **4.6 倍小、且完全不含 `belief`/`stuck`/`life_event` 三个桶（94 个词，占 22%）** 的词表。此时 `tracing::warn!` 只有一行日志，事实产出直接掉一个量级，没有任何硬失败。

### C3 · High — 只能"加"、只能改随包文件：误报关不掉，升级即丢

- 路径写死两个文件名（`markers.rs:23-24`），没有第三个"用户文件"的位置，也没有 env/CLI 指向别处；
- `merge_marker_files`（`markers.rs:35-73`）只做 `pairs.push(...)`，**没有任何删除/覆盖语义**。

后果两连击：

1. 默认词表里在该用户场景下误报的词（例：业务语境里"应酬"是正常工作描述，但它在 `feel` 里），**除了改随包文件没有别的办法关掉**；
2. 改了随包文件，二进制升级一次就被覆盖，用户的定制悄无声息地消失。

### C4 · Medium — 资源根目录可能被 cwd 悄悄劫持；唯一的覆盖机制没写进文档

`config.rs:28-52` 的解析顺序是：`MNEMOSYNE_HOME` → **cwd（只要含 `config/` 或 `lexicon/`）** → exe 所在目录 → cwd。

- cwd 优先于 exe 目录，判定条件又只是"存在 `config/` 或 `lexicon/` 子目录"——只要用户从任何一个带 `config/` 的目录启动安装好的二进制，资源根就整个换掉，然后所有加载器 fail-soft 到内置默认（叠加 C2，静默掉一半词表）；
- `MNEMOSYNE_HOME` 设成不存在的路径**也不会报错**，同样静默降级；
- 这是**唯一**的覆盖机制，`grep -rn MNEMOSYNE_HOME README*.md docs/` **零命中**；`docs/zh/configuration.md` 的环境变量表里也没有它，其正文里 `config/` 字样零命中（也就是说这份"配置"文档完全没讲配置文件）。

### C5 · Medium — 9 个 action 里有 3 个**没有事实类型映射**（22% 的词静默归 Event）

`markers_zh.json` 的 `_meta.actions` 向用户承诺了 9 个 action；`verb_to_fact_type`（`observation_compiler.rs:206-252`）实际只认得 6 个（`喜欢`/`dislike`/`plan`/`want`/`feel`/`准备`），另外 3 个：

| action | `_meta` 里的承诺 | 实际落点 |
|---|---|---|
| `belief` | 观点 / 信念（觉得/认为/相信） | `Event`（26 词） |
| `stuck` | 困难 / 卡点（搞不定/卡住/瓶颈） | `Event`（27 词） |
| `life_event` | 人生大事（搬家/辞职/分手/结婚） | `Event`（41 词，语义上合理） |

也就是说，用户在 `_meta` 读到的"观点/信念"，落地后只会以"最近发生的事件"出现。这不是新缺陷（上一轮语料里 `belief→event` 的"过度抽取"就是它），但它现在是**词表契约的一部分**，做配置化时必须一起定：要么补映射，要么在 `_meta` 里写明"这三类统一落为 Event"。

### C6 · Medium — zh/en 两套词表的 action 命名不统一

| | 只在 zh | 只在 en |
|---|---|---|
| 桶 | `喜欢`、`准备` | `like`、`love` |

同一概念在中文词表叫 `喜欢`、英文词表叫 `like`/`love`，两个文件各写各的 `_meta.actions`。今天能正常工作，靠的是 `verb_to_fact_type` 的硬编码列表同时列了 `喜欢`/`like`/`love`——即"正确的行为"依赖一份手写别名表，而不是词表契约本身。用户新增一个 `fancy` 桶（其实就是 like）会静默变 Event（同 C1）。

附带一项词表自身的质量数据：**跨桶重复词 2 条** —— `下头`（`dislike` + `feel`）、`cringe`（`dislike` + `feel`）。命中后按 action 去重，所以一条消息会产出**两条事实**（偏好 + 情绪）；另有一个单字词条 `呕`（易误匹配，但词表 `_meta` 的 `note` 已声明策略，属可接受）。

### C7 · Low — 新增 4 处 `#[allow(clippy::too_many_arguments)]`，本质是签名过宽

- `src/knowledge/store/world_io.rs:171`（`upsert_world_event_row`）、`:292`（`upsert_world_state_row`）
- `src/knowledge/store/trait_def.rs:160`（`upsert_world_event`）、`:192`（`upsert_world_state`）

trait 与 impl 各写一份，说明签名本身就是 8 参左右（`title, event_type, timestamp, location, …`）。规则 4 允许放着 warning 不管，规则 5 只禁 `dead_code`，所以**这不算违规**；但"为了消 warning 而加 allow、且四处重复"不如**改成参数对象**（`WorldEventRow` / `WorldStateRow`），一次消掉 trait + impl + 调用方四处。另外上一轮刚清空过 `#[allow]`，这一批是回潮，记录下来以免以后当成"历史遗留"。

### C8 · Low — 没有自检通道；两个被引用的文件在仓库里不存在

- 改完词表后，没有任何命令能回答"生效了吗、加载了哪些文件、每个 action 多少词、哪条被忽略了"。`Command` 枚举（`config.rs:474-492`）只有 `Serve` / `Ingest` / `Migrate`；
- `PersonaInjectTool` 指向 `config/persona_cards.json`（`persona_inject_tool.rs:47`），该文件**不在仓库中**；缺失时 `load_persona_cards` 静默返回 `{}`（`persona/inject.rs:194-203`，无日志）；
- `DecayConfig` 指向 `config/decay_config.json`（`decay.rs:28`），同样不存在，靠默认值跑。

单个都不是问题，合起来就是：**"哪些文件是本该有的、现在是缺的"这件事，只能靠读代码得知。**

### C9 · Low — 请求路径上的 `expect`（不变量成立，但 panic-in-handler 本可避免）

`src/mcp/trace_path_tool.rs:149-150`：

```149:150:src/mcp/trace_path_tool.rs
            let (parent, edge) = prev.get(&cur).expect("on path").clone();
            let edge = edge.expect("non-source node has an incoming edge");
```

这两个 `expect` 依赖上面 20 行外 BFS 的簿记（`prev` 只在从 `src` 出发发现邻居时插入 `Entry::Vacant`，因此链一定能回到 `src`）——**当前是正确的**。但它位于工具调用路径上：一旦将来改动 BFS，它是 `panic` 而不是 `Err`。建议改成返回 `Error::Internal`（或 `ok_or_else`），零成本、去掉一个 panic 面。

### C10 · Low — 文档与实现的口径

- `README.zh.md:317-327`「自定义词表」说"直接编辑 JSON 后重启即可" —— 但没告诉用户**改随包文件会被升级覆盖**，也没提 `MNEMOSYNE_HOME`（见 C4）；
- `docs/zh/configuration.md` 通篇没有 `config/` 章节，只有环境变量与 CLI（含 15 个 `MEMORY_*`，缺 `MNEMOSYNE_HOME`）。

### C11 · Low — 三个文件已贴到规则 1 的 1000 行红线

`src/compiler/pipeline.rs` **994**、`src/compiler/profile.rs` **978**、`src/knowledge/store/mod.rs` **964**。三者都在 40 行余量内，其中 `pipeline.rs` 与 `profile.rs` 是 09-24 之后仍在改动的文件 —— **下一次功能增量几乎必然越线**，届时按规则 1 就该做目录化拆分，而不是临时删注释。

## 4. 与"简单配置就能直接用"的差距对照

| 用户想做的事 | 今天的实际结果 |
|---|---|
| 加几个自己的词 | ✅ 可以（编辑 `config/markers_zh.json`） |
| 关掉某个误报的默认词 | ❌ 只能改随包文件（升级丢）；无删除语义（C3） |
| 把词表放在自己的目录 / 自己的文件名 | ❌ 路径写死；`MNEMOSYNE_HOME` 未文档化（C3/C4） |
| 把某个桶改成别的事实类型 | ❌ 不存在这个入口；写错桶名静默变 Event（C1/C5） |
| 确认自己改的生效了 | ❌ 无任何反馈通道（C8） |
| 不改随包文件就完成定制 | ❌ 目前唯一做法就是改随包文件（C3） |

## 5. 建议的修复顺序

### 5.1 与配置目标直接相关（就是之前聊的 4 步）
1. **用户文件叠加 + 删除语义**：`config/` 下 `*.user.json` 参与合并，支持删除默认词（解 C3）；
2. **action 白名单校验 + 单一来源**：非法桶名硬失败并列出合法值，同时把"action → 事实类型"收成一张表（解 C1/C5/C6），并给 `DEFAULT_OBSERVATION_MARKERS` 加一条"必须与随包 JSON 一致"的测试或直接改为最小 fallback（解 C2）；
3. **自检命令**：打印加载了哪些文件、各 action 词数、被删除词条、未知 action / 重复词 / 空词条（解 C8，顺带把 C6 的 2 条重复词变成可见告警）；
4. **文档**：`docs/zh/configuration.md` 补 `config/` 与 `MNEMOSYNE_HOME` 章节，README 补"升级会覆盖随包文件，定制请放 `*.user.json`"（解 C4/C10）。

### 5.2 独立小修（与配置无关，可单独一次做完）
- C9：`trace_path_tool` 两个 `expect` 改错误返回；
- C7：`upsert_world_{event,state}` 改参数对象，删掉 4 处 `#[allow]`；
- C6 数据项：`下头` / `cringe` 从两个桶里择一（这属于词表数据，改了就改了，需要你确认放哪边）。

### 5.3 两个 clippy warning（规则 4 允许不管，这里是修法备查）
均位于 `src/persona/inject.rs`，lint 为 `collapsible_match`：

- `:161`（`FactType::Relationship` 分支）与 `:155` 同构，改成带 guard 的分支即可，语义不变（guard 不成立时落到 `_ => {}`）；
- `:268`：`(_, Value::Object(_)) | (_, Value::Null) => { if !r.is_null() { … } }` 可塌缩为 `(_, Value::Object(_)) if !r.is_null() => …`，`Null` 落到 `_ => {}`，语义不变。

## 6. 本轮**未**审查的部分（上次评审后被改动的文件）

按 mtime 列出（用于界定我上一轮结论的时效边界，不代表这些改动有问题）：

`src/knowledge/store/{mod,trait_def,types,world_io,tests/mod}.rs`、`src/storage/schema.rs`、`src/compiler/{pipeline,mod,state_slots,extract,profile}.rs`、`src/knowledge/memory_export.rs`、`src/ingest/relation.rs`、`src/persona/check.rs`、`src/agent_facts.rs`、`src/mcp/{knowledge_tools,http_server,external_knowledge_tools}.rs`、`src/compiler/name_validation/mod.rs`、`src/lib.rs`、`config/markers_zh.json`。

这批量不小（world 模型/导出/编译管线方向），我只做了机器扫查：门禁绿、无超长文件、无新增 `dead_code` 抑制。**要单独开一轮语义审查的话，请指明优先级**（我建议 world_io / state_slots / memory_export 这三个先看，因为它们是新增面）。

## 7. 本轮未做（明确声明）

- 未执行任何 `git` 命令（规则 6）；
- 未删除任何文件、未执行任何 `rm`（规则 1）。

## 8. 修复记录（2026-09-25，按 `plan/rules/rules.md` 执行）

### 8.1 词表机制（这是本轮的主体）

| 编号 | 修法 | 涉及文件 |
|---|---|---|
| C1 | 桶名白名单：`OBSERVATION_ACTIONS` 成为唯一来源，未知桶**丢弃并报错**（不再静默变 `Event`） | `src/observation_compiler.rs`、`src/conversation_compiler/markers.rs` |
| C2 | 内置兜底表从"JSON 的第二份拷贝"改为**明确的小型安全网**（26 条，覆盖全部 13 个动作），并加测试钉住：兜底词必须是随包表的子集、动作必须是已定义动作 | `src/conversation_compiler/markers.rs` |
| C3 | `config/*.user.json` 叠加（按文件名顺序）+ `_remove` 删除语义；删除未命中也会被报告 | 同上 |
| C5 | `belief` / `stuck` / `life_event` 三个动作在表里**显式**声明为 Event，并在 `_meta` 与工具输出里写明，不再靠 `_` 兜底 | `src/observation_compiler.rs`、`config/markers_*.json` |
| C6 | 别名关系显式化（`like`/`love`/`prefer`/`hate` → Preference 并标注为中文桶的英文别名）；跨桶重复词变为**自检里的可见告警**；随包表里 `afraid` 的桶内重复已删除 | 同上 |
| C8 | 新增 `mnemosyne config-check`：打印资源根目录、每个词表文件贡献多少词、每个动作最终词数、被删/被忽略/重复条目，以及运行时会读取的每个 `config/` 文件是否存在；词表不可信时退出码 1 | 新增 `src/config_check.rs`、`src/config.rs`（`Command::ConfigCheck`）、`src/main.rs` |
| C4/C10 | `docs/zh/configuration.md` 新增「资源文件（`config/`）」章节（含 `MNEMOSYNE_HOME` 解析顺序与文件清单）；README 中英双语的「自定义词表」改写为"词是你的、动作名是引擎的"，并说明升级覆盖问题；两个随包词表的 `_meta` 增加 `user_tables` 说明 | 文档 + `config/markers_*.json` |

### 8.2 独立小修

- **C9**：`src/mcp/trace_path_tool.rs` 两个 `expect` 改为 `Error::Internal`（不变量成立，但请求路径上不应 panic）。
- **§5.3 两个 clippy warning**：`src/persona/inject.rs` 的 `collapsible_match` 修掉（改为带 guard 的分支 + 删掉恒真的 `!r.is_null()` 分支），语义不变。

### 8.3 未做（需要你决定或属于另一轮）

| 编号 | 原因 |
|---|---|
| **C7** | `#[allow(clippy::too_many_arguments)]` ×4 属参数对象重构（`upsert_world_event` / `upsert_world_state` 的 trait + impl + 调用方），落在你今天还在改的 V7 world 代码里；规则 4 明确允许先不管 warning，我不在没有你确认的情况下动你的在写代码。你说一句我就改。 |
| **C11** | `pipeline.rs` 994 / `profile.rs` 978 / `knowledge/store/mod.rs` 964 行，只是贴线提醒，无动作可做（真越线时按规则 1 拆目录）。 |
| **词表数据** | 跨桶重复词 `下头` / `cringe`（`dislike` + `feel`）保留原样：放哪个桶是**语义决定**，不是缺陷。现在 `config-check` 会把它们列出来，你决定后改一行 JSON 即可。 |

### 8.4 验证结果

| 检查 | 结果 |
|---|---|
| `cargo fmt --all --check` | ✅ 干净 |
| `cargo clippy --all-targets --all-features` | ✅ 0 error / 0 warning（C7 的 4 处 allow 仍在，但它们不再产生 warning） |
| `make test` | ✅ **839 passed / 0 failed / 9 skipped**（本轮 +15 个测试） |
| `mnemosyne config-check`（本仓库） | ✅ 419 词 / 11 个动作，退出码 0，无重复、无未知动作 |
| `MNEMOSYNE_HOME` 端到端（临时目录） | ✅ 用户表被发现并叠加（`want` +1）、`_remove` 生效（`feel` −1）、写错桶名进 errors 且退出码 1 |
| 单文件 ≤1000 行（规则 1） | ✅ 最大 `src/compiler/pipeline.rs` 994（未变） |

新增 15 个测试：`markers.rs` 10 个（未知桶、用户表叠加、`_remove` 命中/未命中、跨桶、不可用条目、兜底子集、随包桶全覆盖、动作表唯一性与文档、`.user.json` 目录扫描、后文件可恢复被删词）+ `config_check.rs` 5 个（错误/警告/缺文件/随包自检/资源清单）。
其中 `.user.json` 目录扫描测试当场抓到一个真实缺陷：目录名以 `.user.json` 结尾时会被当成词表并被读取（已在 `user_tables_in` 加 `is_file()` 过滤）。
