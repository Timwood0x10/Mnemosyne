# 全库 Code Review（第二轮）— 认知状态层实现与拆分后的回归面

> 评审日期：2026-09-23 · 版本：`0.1.3` · **状态：R1–R14 已全部修复（见 §6）**
> 范围：`src/` 全量（重点：`state.rs`、`cognition/`、`fact_store/`、`decision*`、`commitment.rs`、
> `mcp/{state_timeline,provenance,decision}_tool.rs`）+ 模块拆分后的公共 API 面。
> 方法：静态通读 + 关键路径**生产数据形状穷举**（不依赖测试里手工构造的 payload）+ 公共 API 可达性核对。
> 本文件为只读评审记录，不参与构建。

## 0. 结论

拆分（规则 1）与上一轮 6 项缺陷修复**均已验证有效、无回归**。但本轮评审发现认知状态层有一个更根本的
问题：**"变迁（Transition）"能力在真实生产数据上基本不可达**（三种类型里 `gradual_change` 100% 死代码、
`stance_flip` 对用户实体不可达），且 `memory_decay` 的产出没有任何消费者。
换言之，v0.3 的招牌叙事（"三状态演化链"、"有管理的遗忘"）当时只在测试里成立。

**R1–R14 已全部修复**（§6）：核心是让变迁判定基于生产 payload 真正携带的字段、让用户否定进入事实流
（用户侧 `stance_flip` 已在真实 MCP 链路上跑通），并堵住 decision 写入的原子性、租户作用域与迁移
NULL 安全三处风险。`memory_decay` 的定位已改为诚实的"策展信号"（§7 遗留其一）。

| 级别 | 数量 | 主题 |
|---|---|---|
| High | 5 | 变迁类型生产不可达（R1）、用户否定被丢弃（R2）、衰减无消费者（R3）、折叠键不稳定（R4）、证据链未落库（R15） |
| Medium | 4 | 迁移非 NULL 安全（R5）、承诺抽取假阳性（R6）、decision 写入非原子（R7）、新工具无租户作用域（R8） |
| Low | 6 | 伪造 fact_id（R9）、陈旧/悬空文档（R10/R11）、维度清单重复（R12）、折叠键口径不一致（R13）、规则 5 违背（R14） |

修复状态一览（明细见 §6）：

| 编号 | 修复要点 | 状态 |
|---|---|---|
| R1 | `GradualChange` 改为「同一话题内取值变化」，比较文本支持 `content`→`object`→`keyword`→`action` 回退 | ✅ |
| R2 | 否定观测不再整条丢弃；用户侧 `stance_flip` 在真实 MCP 链路上可复现 | ✅ |
| R3 | 缺失 `access_count` 不再等同「从未访问」；模块定位与文档改为「策展信号」 | ✅ |
| R4 | 维度键表新增稳定 `keyword`；计数器字段不再进入折叠键 | ✅ |
| R5 | 迁移改 `COALESCE(weight, 1.0)`，NULL 行不再让 store 打不开 | ✅ |
| R6 | 标记词边界 + 否定窗口，`compromise`/`I will not…` 不再产生假决策 | ✅ |
| R7 | 一次 `insert_compilation` 事务提交 facts + 锚点 + decisions | ✅ |
| R8 | 四个工具新增可选 `tenant_id` 并做归属校验（不符返回 not found） | ✅ |
| R9 | `fact_ids` 只记录真实 id，不再伪造 `0` | ✅ |
| R10/R11 | 陈旧描述与悬空链接全部更正 | ✅ |
| R12 | 工具维度清单由 `COGNITIVE_DIMENSIONS` 派生 | ✅ |
| R13 | 值键/话题键合并进单张 `COGNITIVE_DIMENSIONS` 表 | ✅ |
| R14 | 三处 `#[allow]` 全部移除（无新增告警）；库内告警改 `tracing::warn!` | ✅ |
| R15 | 编译期登记 `evidence` 行并写回 `evidence_id`；Step 2 验收（三状态 + 各自证据）真实数据通过 | ✅ |

---

## 1. High

### R1 —— `gradual_change` 在生产链路上**永远不可能**产生（招牌能力是死代码）

`detect_transition` 的入口条件与分支条件互相**不可能同时满足**：

```230:232:src/state.rs
    if from_content.is_empty() || to_content.is_empty() {
        return None;
    }
```

```261:266:src/state.rs
    if from_content != to_content
        && from.value.get("keyword").is_some()
        && to.value.get("keyword").is_some()
    {
        return Some(TransitionType::GradualChange);
    }
```

- `content_of`（`src/state.rs:272-277`）**只读 `payload["content"]`**。
- `GradualChange` 又要求两侧 payload 都含 `keyword`。

穷举生产端 payload 形状（逐处核对赋值点）：

| 生产通道 | payload | 有 `content` | 有 `keyword` |
|---|---|---|---|
| 用户 observation（`conversation_compiler/mod.rs:386-408`） | `{action, subject, object, content, evidence?}` | ✅ | ❌ |
| companion 主题（`knowledge/companion_extract.rs:225-229`） | `{keyword, occurrences, samples}` | ❌ | ✅ |
| companion 身份（同上 `:203-206`） | `{content, turn}` | ✅ | ❌ |
| agent 人设（`agent_personality.rs:183`） | `{attribution, content, negated}` | ✅ | ❌ |
| 观察规则输出（`observation_compiler.rs:183-187`） | `{action, subject, object}` | ❌ | ❌ |

**没有任何生产 fact 同时具备 `content` 与 `keyword`** → `GradualChange` 恒不触发。
计划 §4 Step 2 自己举的例子（"喜欢独处 → 开始想社交 → 喜欢热闹"）无法复现。

测试为什么全绿：`src/state.rs:598-599`、`:651` 与 `src/mcp/state_timeline_tool.rs:270-322`
全部由测试**手工注入 `keyword`**——与上一轮 H1/H2 的盲区同源。

**修法（择一，建议第 1 种）**：
1. `content_of` 增加回退：`content` → `object` → `keyword`，并用 `action`/`keyword` 参与比较；
2. 让编译期统一补齐 `keyword`（观测的语义主题），使三种变迁都能基于同一可比文本判定。

### R2 —— 用户侧否定语句被整条丢弃，用户实体的 `stance_flip` 结构性不可达

```314:322:src/conversation_compiler/mod.rs
            let mut negated = false;
            ...
                    "negation" => negated = true,
```

```345:350:src/conversation_compiler/mod.rs
                if negated {
                    modifiers.push(crate::cognition::Modifier {
                        key: "negated".to_string(),
```

```380:383:src/conversation_compiler/mod.rs
        // Skip observations whose source message was negated
        // (ELITE_LEXICON_PLAN §13.3: "I do not plan to X" must not produce an
        // affirmative Goal). Uncertain statements are kept but tagged.
        .filter(|observation| !has_modifier(observation, "negated"))
```

- 检测到否定后**整条 observation 被丢弃** → 用户说"我不喜欢应酬"**不产生任何 fact**。
- 而 `StanceFlip` 要求两侧 payload 都有 `negated` 且不同（`src/state.rs:244-255`）。
- 生产端唯一写 `negated` 的是 `agent_personality.rs:183`（**agent 实体**）与 `persona/*`（人设事实）。
  用户通道（observation + companion）没有任何 `negated: true`。

后果两点，都比"少一个变迁"严重：

1. 对最常见的查询对象（用户）而言，三种变迁里 `StanceFlip` 恒不触发；
2. **引擎无法表达用户的否定偏好**——"不喜欢 X"这条信息在编译期即被静默丢弃，
   与"ADD-only / facts 是唯一真相"的定位相冲突。

**修法**：保留否定观测，产出 `payload["negated"] = true` 的 fact（并把"不得生成肯定 Goal"
的约束下沉到规则层：否定时把类型降级为 `Preference`/`Emotion` 或打 `negated` 标记，
而不是删掉整条）。这样 `StanceFlip` 才会自然成立。

### R3 —— 衰减状态没有任何消费者，`memory_decay` 对可观测行为零影响

写入方：`set_decay`（`src/fact_store/mod.rs:281-288`）写 `weight`/`archived`。
读取方（全库穷举）：`get_decay`（`:387`）、`list_archived`（`:295`），
生产侧仅 `src/decay.rs:357-358` 的薄包装；`Fact` 结构体**不暴露** `weight`/`archived`。

即：`read_facts` 不按 `weight` 排序、`state_timeline`/`cognitive_context`/`persona_*`
都不过滤 `archived`。于是

- `src/decay.rs:1-7` 的承诺 "Decay reduces a stored fact's retrieval influence over time" **不成立**；
- `archived` 是死标志，`memory_decay` 返回的 `archived: N` 会误导调用方；
- 叠加下面这条更显得危险：

```218:218:src/decay.rs
    let should_archive = decay_score < 1.0;
```

默认 `Hybrid` 策略下，任何 `access_count` 缺失（默认 0）的 fact，
`access_score = 1 - (1-0)*(1-0.5) = 0.5` → 综合分 < 1.0 → **首次 decay 就把几乎所有 fact 标记为 archived**。

**修法**：明确二选一——(a) 让消费者到位（检索/上下文按 `weight` 排序、按 `archived` 降权）；
(b) 承认 decay 只是"导出/审计用的派生标记"，修正模块文档与工具描述，并把 `should_archive`
改成显式阈值（如 `decay_score < config.decay_factor`）而不是"任何不足 1.0"。

### R4 —— 折叠键不稳定：计数器类字段把"同一状态"炸成多个区间

```215:221:src/state.rs
fn resolve_state_value(payload: &serde_json::Value, value_keys: &[&str]) -> serde_json::Value {
    value_keys
        .iter()
        .find_map(|key| payload.get(*key))
        .cloned()
        .unwrap_or_else(|| payload.clone())
}
```

回退策略是**整份 payload**（上一轮修 H2 时引入，用于避免"全部折叠成一个区间"）。但对
companion 主题 fact（`{keyword, occurrences, samples}`）而言，`value_keys` 一个都不命中 →
`state_value` = 含 `occurrences`（出现次数）与 `samples`（采样文本）的整份 payload。

同一个偏好每编译一次 `occurrences` 就变 → `fold_key` 变 → **同一状态被切成 N 个区间**，
`state_timeline` 报告大量伪"状态变化"，与 ADD-only 的"区间=状态有效期"语义不符。

**修法**：把 `keyword` 补进 `COGNITIVE_DIMENSIONS` 的值键列表（`Preference` 已有 `topic`，
可并到同一优先级），并让回退只取**稳定字段子集**（如 `keyword`/`object`）而非整份 payload。

### R15 —— `evidence` 表在生产路径上从未被写入：`fact_provenance` 的"证据原文"恒为 `null`

`insert_evidence` 在全库的**唯一调用点在测试里**（`src/mcp/provenance_tool.rs:262`），
即生产代码没有任何地方向 `evidence` 表写数据。

而编译期把原文放在 payload 里：

```386:408:src/conversation_compiler/mod.rs
                fact.payload["evidence"] = serde_json::json!({
                    "doc_id": evidence.doc_id,
                    "offset": evidence.offset,
                    "length": evidence.length,
                    "text": evidence.text,
                });
```

`Fact.evidence_id` 始终是 `None`（`observation_compiler.rs:195` 等），所以 `facts.evidence_id`
落库即 NULL。后果：

- `fact_provenance` 的 `evidence` 对**任何真实事实**都是 `null` —— Step 1 的
  "为什么这么认为"在生产数据上问不出答案；
- `state_timeline` 每个区间的 `evidence_ids` 恒为**空数组** —— Step 2 验收明确要求
  "三个时间状态及各自 evidence"；
- `evidence` 表沦为只被测试写入的死存储，而 `docs/zh/mcp-tools.md` 仍在描述它。

**修法**：写入路径识别 `payload.evidence.text`，在同一事务内登记证据行并写回 `evidence_id`；
同一条话语产生的多条事实**共享一行**（避免 4 条事实 = 4 行重复）；`doc_id: 0`（非文档来源）存 NULL。

---

## 2. Medium

### R5 —— `confidence` 迁移非 NULL 安全（最坏导致服务起不来）

```153:155:src/fact_store/mod.rs
        if Self::ensure_column(conn, "facts", "confidence", "REAL NOT NULL DEFAULT 1.0")? {
            conn.execute("UPDATE facts SET confidence = weight", [])?;
        }
```

`weight REAL DEFAULT 1.0` 是**可空列**。若历史库中有任意一行 `weight IS NULL`，这条 UPDATE
会写 NULL 到 `NOT NULL` 列 → 报错 → `SqliteFactStore::open` 失败 → 服务无法启动（不是降级，是起不来）。

**修法**：`UPDATE facts SET confidence = COALESCE(weight, 1.0) WHERE weight IS NULL OR confidence = 1.0`。

### R6 —— 承诺抽取是纯子串匹配且不处理否定，会造出**语义相反**的决策

```64:68:src/commitment.rs
    let lowered = message.content.to_lowercase();
    let verb = COMMITMENT_MARKERS
        .iter()
        .find(|(marker, _)| lowered.contains(&marker.to_lowercase()))
```

子串匹配导致的必然假阳性：

| 输入 | 命中 | 结果 |
|---|---|---|
| `we reached a compromise` | `promise`（`compromise` 的子串） | 生成 `promise` 决策 |
| `I will not help you` | `i will` | 生成 `commit` 决策（语义相反） |
| `我保证不去` | `保证` | 生成 `commit` 决策（语义相反） |

对审计层（`decision_trace` 的定位是"为什么这么做"）而言，**造出假决策比漏掉真承诺更糟**。

**修法**：加词边界/前后缀约束（英文用词边界、中文要求标记前无否定词），并复用
`FUNCTIONAL_MATCHER` 的 `negation` 语义类做否定短路。

### R7 —— `memory_compile` 的 decision 写入非原子，失败会留下半成品

```92:130:src/mcp/memory_compile.rs
        let mut stored_facts = self.fact_store.insert_batch(&compiled.facts)?;
        ...
        for (role, subject) in self.commitment_speakers(args, tenant_id, user_entity_id)? {
            ...
                let anchor_id = self
                    .fact_store
                    .insert_fact(&crate::commitment::anchor_fact(&decision, logical_time))?;
                ...
                self.fact_store.insert_decision(&decision)?;
```

facts 已在 `insert_batch` 中提交；其后 `commitment_speakers`（可能 `resolve_agent` 失败）、
`insert_fact`（锚点）、`insert_decision`（`validate_decision` 可能拒绝）任一步失败，
工具返回 `Err` 但 **facts 已落库**；若失败发生在 `insert_fact` 与 `insert_decision` 之间，
还会留下**孤立锚点 fact**。调用方重试 → 事实重复。

**修法**：把 facts 写入 + 锚点 + 决策放进同一个 `store.transaction`（`insert_batch` 已有事务模式可复用），
或把 commitment 抽取与校验**前置**到任何写入之前（先用纯函数算出全部待写内容并校验，再一次性提交）。

### R8 —— 四个新工具只接受裸 id，无租户作用域

- `state_timeline(entity_id)`、`fact_provenance(fact_id)`、`decision_trace(decision_id)`、
  `decision_search(subject)` 均不校验 `tenant_id`；
- `decisions` 表甚至没有 `tenant_id` 列（`src/fact_store/mod.rs:80-90`）。

上一轮专门修过 `memory_decay` 的跨租户扫描（`all_entity_ids_in_tenant`，`fact_store/mod.rs:363-377`），
这里口径不一致：知道（或猜到）别租户的 entity/fact id 即可读取其事实与决策。

**修法**：给四个工具加可选 `tenant_id`，在 store 层校验 `entity_id`/`fact_id` 归属；
`decisions` 增加 `tenant_id` 列与联合索引（幂等迁移）。

---

## 3. Low

| 编号 | 位置 | 问题 | 修法 |
|---|---|---|---|
| R9 | `src/state.rs:158,175` | `fact.id.unwrap_or(0)` 把未持久化的 fact 记成 `fact_ids: [0]`，对外暴露一个不存在的证据 id | 改成 `if let Some(id) = fact.id { push(id) }` |
| R10 | `src/cognition/mod.rs:163-164`、`src/mcp/provenance_tool.rs:6` | 仍写 "`confidence` mapped from the legacy `weight` column (decay down-weights it)"——L3 拆列后既非映射、衰减也不再改写它 | 改为"独立列，与衰减正交" |
| R11 | `src/mcp/decision_tool.rs:4`、`docs/zh/external-knowledge.md:421` | 悬空链接：`plan/cognitive-state-v03.md`、`plan/external-knowledge-plan.md` 均不存在（`plan/` 只有 `postmortem.md` + `rules/`） | 指向 `docs/zh/dev-plan-cognitive-state.md` / 补文件或移除 |
| R12 | `src/mcp/state_timeline_tool.rs:37-38` | `STATE_TIMELINE_DIMENSIONS` 与 `src/state.rs:304-316` 的 `COGNITIVE_DIMENSIONS` 是两份手工维护的数据；将来加维度时工具会以 `InvalidInput` 拒绝合法输入 | 由 `COGNITIVE_DIMENSIONS` 派生（`iter().map(...)`） |
| R13 | `src/cognition/state.rs:102-132` vs `src/state.rs:304-316` | 同一维度的折叠键优先级不一致（如 preference：`["topic","preference","content"]` vs `["content","preference","topic"]`；emotion 在 `aggregate` 里是"取全部 Emotion 事实"，在历史里按 content 折叠） | 抽出单一 `DIMENSION_KEYS` 常量，两处共用 |
| R14 | `src/config.rs:530`、`src/ingest/mod.rs:244`、`src/dictionary.rs:142`；`src/conversation_compiler/markers.rs:40,47,70` | 规则 5：存在 `#[allow(...)]` 抑制；`markers.rs` 用 `eprintln!` 报 warning（`decay.rs` 用的是 `tracing::warn!`，口径不一） | 去掉 allow（改为真正消除告警）；`eprintln!` 换 `tracing::warn!` |

---

## 4. 已核查**未发现**问题的项（避免误报，记录覆盖范围）

- **拆分后公共 API 可达性**：`knowledge/store/{types,trait_def}.rs`、`cognition/{state,snapshot}.rs`、
  `lexicon/{matcher,registry}.rs`、`distiller/{pipeline,text}.rs`、`character/store.rs`
  的 `pub` 项均在各自 `mod.rs` 中重新导出或本就在 `mod.rs`；
  `InspectEntityResult`/`TimelineEntry`/`RelationGraphResult`/`EvidenceHit` 定义在 `knowledge/mod.rs`，未受影响。
- **`knowledge/store` 单 trait impl 拆分**：7 个大方法体原样下沉为 `pub(super)` 固有方法 + 一行委托，
  `self` 语义未变；`search_evidence_query` 的 LIKE 转义与 `limit.min(10_000)` 钳制正确。
- **`entities` 去重**：`CREATE UNIQUE INDEX ... ON entities(tenant_id, external_key) WHERE external_key IS NOT NULL`
  （`fact_store/mod.rs:160-165`）存在，`resolve_entity` 的 read-then-insert 并发不会静默产生重复用户。
- **`update_relationship_atomic`**：单次持锁内完成 load→compute→save（`fact_store/relationship.rs:53-66`），
  不存在上一轮 M2 那类"读—判—写"竞态。
- **`set_decision_outcome`**：守卫已下推 SQL（`WHERE id = ?2 AND outcome IS NULL`），原子性有测试锁死。
- **`search_decisions`**：转义顺序正确（先 `\` 再 `%`/`_`）+ `ESCAPE '\'`。
- **`#[allow]`/`println!` 之外**：无 `dbg!`、无非测试 `unwrap()`（`vector/*`、`value_extract.rs` 的命中均在 `mod tests` 内）。
- **规则 1**：`src/` 下无 >1000 行文件。

---

## 5. 建议的修复顺序（按"对定位的损害程度"）

1. **R2**（用户否定被丢弃）—— 这是信息丢失，且是 R1/StanceFlip 的根因之一；
2. **R1**（变迁不可达）+ **R4**（折叠键不稳定）—— 一起改 `state.rs` 的"可比文本/折叠键"口径，
   补一条**用真实编译产物**（`memory_compile` → `aggregate_intervals`）的变迁回归测试；
3. **R3**（衰减无消费者）—— 需要产品决策：补消费者，还是把 decay 定位改成"审计标注"；
4. **R5**（迁移 COALESCE）—— 一行，防"服务起不来"；
5. **R6/R7**（承诺抽取与写入原子性）；
6. **R8**（租户作用域）—— 涉及 schema，建议与 `decisions` 一起迁；
7. R9–R14 收尾。

## 6. 修复记录（R1–R14，按 `plan/rules/rules.md` 执行）

### 6.1 状态层：R1 / R4 / R9 / R12 / R13

- `src/state.rs`
  - `detect_transition` 重写：`state_text` 提供 `content`→`object`→`keyword`→`action` 的回退比较文本；
    `TOPIC_KEYS`（`keyword`/`topic`/`preference`/`action`）判定「同一话题」，取代原先「两侧都必须有
    `keyword`」的死条件；信号强度排序改为 `stance_flip` > `behavioral_confirmation` > `gradual_change`。
    **不再有任何生产 payload 无法满足的分支**。
  - `resolve_state_value` 只接受字符串字段（与 `latest_by_payload_key` 同一规则）；`fact_ids` 只记录真实 id。
  - `COGNITIVE_DIMENSIONS` 改为 `CognitiveDimension { fact_type, value_keys, topic_keys }` 单表：
    历史层按 `value_keys`（值优先）折叠、当前态按 `topic_keys`（话题优先）取最新，
    并新增 `dimension_value_keys` / `dimension_topic_keys` 供两个投影共用。
- `src/cognition/state.rs`：`aggregate` 的键优先级改为从该表读取（R13 消除两份手写清单）。
- `src/mcp/state_timeline_tool.rs`：`STATE_TIMELINE_DIMENSIONS` 常量改为
  `state_timeline_dimensions()` 函数，由 `COGNITIVE_DIMENSIONS` 派生（R12）。
- 6 个变迁测试移到 `tests/state_transitions.rs`（规则 4.2：调用公共 API），
  使 `src/state.rs` 回到 **761 行**（规则 1）。

### 6.2 编译链路：R2

- `src/conversation_compiler/mod.rs`
  - 取消「否定观测整条过滤」；改为按 `FactType` 处理：`Goal` 仍丢弃（保住 §13.3），
    其余类型保留并始终写入 `negated: true/false`（肯定侧也显式声明，才能使 `StanceFlip` 两侧可读）。
- `tests/cognitive_state_e2e.rs` 新增 `user_stance_flip_is_reachable_over_mcp`：
  真实 `tools/call` 下先编译「我喜欢应酬」再编译「我不喜欢应酬」，
  `state_timeline` 返回唯一且为 `stance_flip` 的变迁 —— 修复前这是**结构性不可能**的。

### 6.3 决策层：R6 / R7 / R8

- `src/commitment.rs`：ASCII 标记必须起始于词边界（`compromise` 不再命中 `promise`，`promises` 仍命中）；
  新增否定判定（同子句前置否定 + 标记后 3 字窗口），`我保证不去` / `I will not help you` / `I promise nothing`
  均不再产生决策；动词取文本中**最早**的标记。
- `src/fact_store/`：
  - 抽出 `FACT_COLUMNS` / `INSERT_FACT_SQL` / `bind_fact` / `get_facts_on`，消除四处重复 SQL；
  - 新增 `insert_compilation`：一次事务写入 facts + 锚点 + decisions，写入前先校验全部决策
    （`validate_decision` 真正接进写路径），失败则**零残留**；
  - `insert_decision_on` 供事务内复用。
- `src/mcp/memory_compile.rs`：改为「先全部准备好（`compiled_commitments`，含 agent 实体解析）→
  再一次事务提交」，不再有半成品写入。
- 租户作用域：新增 `src/mcp/tenant_scope.rs`（`tenant_argument` + `ensure_entity_tenant`），
  `state_timeline` / `fact_provenance` / `decision_trace` / `decision_search` 四个工具全部接入可选
  `tenant_id`；归属不符返回 `NotFound`（不泄露「该 id 存在」）。四个工具的 schema 与
  `README.md` / `README.zh.md` / `docs/{zh,en}/mcp-tools.md` 同步更新。

### 6.4 衰减与迁移：R3 / R5

- `src/fact_store/mod.rs`：`UPDATE facts SET confidence = COALESCE(weight, 1.0)`。
- `src/decay.rs`：`extract_access_count` 改为 `Option<u64>`，缺失即「访问史未知」→ 中性 1.0，
  显式 `access_count: 0` 仍然衰减；模块文档与 `should_archive` 语义改为「已下调并记分，永不隐藏」。
  修复了「首次 decay 就把几乎所有 fact 标记为 archived」。

### 6.5 文档与告警：R10 / R11 / R14

- `src/cognition/mod.rs` 的 `confidence` 描述、`src/mcp/provenance_tool.rs` 的 `legacy weight` 描述已更正。
- `src/mcp/decision_tool.rs` 的 `plan/cognitive-state-v03.md`、
  `docs/zh/external-knowledge.md` 的 `plan/external-knowledge-plan.md` 悬空链接已修正。
- 移除全部 `#[allow(...)]`（`config.rs` 测试改为结构体更新语法；`ingest/mod.rs` 的
  `too_many_arguments` 与 `dictionary.rs` 的 `dead_code` 属陈旧抑制，直接删除后无告警）；
  库内 5 处告警 `eprintln!`（`domain_profile.rs`、`dictionary.rs`、`lexicon/mod.rs`、`entity_resolver/embedding.rs`、
  `conversation_compiler/markers.rs`）改为 `tracing::warn!`。

### 6.7 追加修复：R15（证据链落库 + Step 2 验收）

- `src/fact_store/mod.rs`
  - `bind_fact` 接受显式 `evidence_id`；新增 `insert_facts_on`（共享 `anchors` 缓存的批量写入）
    与 `anchor_evidence_on`（把 `payload.evidence.text` 登记为 `evidence` 行并返回 id）。
  - `insert_fact` / `insert_batch` / `insert_compilation` 三条写入路径统一走这套逻辑，
    因此**同一条话语产生的多条事实共享一行证据**，`doc_id: 0` 存 NULL，无 payload 锚点的事实不动。
- `src/fact_store/decisions.rs`：`insert_compilation` 复用同一缓存，锚点事实与编译事实在同一事务内落库。
- `tests/cognitive_state_e2e.rs::three_state_evolution_chain_carries_its_evidence`：
  用**真实编译器**（`compile_user_facts`，逐步指定逻辑时间）编译
  「我喜欢一个人待在家里 → 我喜欢和朋友一起聊天 → 我喜欢参加热闹的聚会」，
  再走真实 `tools/call` 断言：
  1. 三个区间 `(2024,2025)/(2025,2026)/(2026,null)`（**三个状态都保留**）；
  2. 每个区间各自携带**唯一且可读回**的 `evidence_ids`；
  3. 变迁类型为 `gradual_change` → `behavioral_confirmation`（能证明的才报）；
  4. `fact_provenance.evidence` 能读回第一状态的原文。
- `src/fact_store/mod.rs` 单测 `compiled_facts_register_their_evidence_anchor`：
  共享单行、可读回、`doc_id` 为 NULL、无锚点事实不伪造。

### 6.8 验证结果

| 检查 | 结果 |
|---|---|
| `cargo fmt --all --check` | ✅ 干净（FMT_CLEAN） |
| `make check`（clippy + check，`--all-targets --all-features`） | ✅ 0 error / 0 warning |
| `make test`（nextest，lib + 全部集成测试） | ✅ 801 passed / 0 failed / 9 skipped |
| 单文件行数（规则 1） | ✅ `src/` 与 `tests/` 均无 >1000 行（最大 `src/state.rs` 761） |
| `#[allow(...)]` 抑制（规则 5） | ✅ 0 处 |

新增/调整测试 12 个（含 6 个变迁集成测试 + 4 个租户作用域 + 否定状态 + 决策原子性），
全部带 `Objective` / `Invariants` 注释与带上下文的断言消息。

## 7. 遗留（明确未做）

- **Decision 在生产上永远停在 `Open`。** 全库核对：`set_decision_outcome` 的唯一调用点在
  `src/fact_store/decisions.rs` 的测试里，没有任何工具或编译路径会写 `outcome`。
  这不是接线疏漏，而是计划 §4 Step 3 把 MCP 表面积冻结成「两个只读工具」的直接后果——
  **"什么事件算兑现/违约"是产品语义决策**（新增一个工具？编译期从"我做到了/我没去"推断？运维接口？），
  计划未定义，因此不擅自发明。
- **`memory_decay` 的产出仍无检索侧消费者**：本轮把「几乎全部 fact 被判为已衰减」的评分错误修好，
  并把文档/工具描述改成诚实的「策展信号」，但**没有**让检索或上下文按 `weight`/`archived` 排序或过滤。
  这是有意的：dev-plan §4 Step 3 第 4 条已冻结「是否纳入 decay 留待后续根据实际数据决定，现在不提前设计」，
  而 `aggregate` 的「最新优先」语义也不应被 decay 偏置。
- **证据行按"每次编译"累积**（同一条话语重复编译会产生新行，与事实的重复语义一致）。
  若要收敛，可改为内容寻址去重；当前未做，避免引入跨编译的隐式复用。
- **公共 API 中的薄包装未被生产调用**：`RelationshipStore::upsert_relationship`（无人调用）、
  `decay::archive_fact`（仅测试调用，`run_decay_pass` 直接走 `set_decay`）。
  属既存公共 API，删除需你确认，本轮只记录。
- **`decisions` 表仍无 `tenant_id` 列**（R8 的租户隔离通过 `subject` 实体归属校验实现）。
  已确认：本项目定位为单机 MCP 服务，租户隔离非当前优先级，故不动 schema。
- **`ingest/mod.rs` 的 `eprintln!` 进度输出**（`ch{} t=… ms`）与 `main.rs` 的 CLI 输出保留：
  它们是 CLI 诊断而非库噪声，改成 tracing 需要 CLI 侧配置订阅者，属行为变更，未纳入本轮。
- 未执行任何 `git` 命令（规则 6）；未删除任何项目文件、未执行任何 `rm`（规则 1）。
  （为满足规则 1，本轮以「新增 `tests/state_transitions.rs`」与「新增 `src/fact_store/evidence.rs`」
  而非删除源文件的方式回到 1000 行以内。）
