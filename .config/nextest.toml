# cargo-nextest configuration
# Docs: https://nexte.st/docs/configuration/

# The `serial-db` test group previously serialized tests sharing the
# `/tmp/lorescope_sanguo.db` fixture. Those corpus-dependent tests were
# removed (corpus/ is gitignored and never present in CI), so no test group
# overrides are needed anymore.
